# 产品管理

目标是产生一个人和AI共同维护的文档系统，从用户故事开始，到代码实现、测试、发布，形成一个由确定的流程主导的，人和AI共同参与的文档系统。

## 代码

代码的部分，使用了python，并且使用uv来管理python环境。
使用uv add来添加python依赖，禁止使用pip。
使用uv run --env-file .env main.py来运行项目。
.env文件中配置了PYTHONIOENCODING=utf-8，确保中文输出正常。
运行测试使用uv run --env-file .env pytest。
每个模块是一个独立的python文件，每个方法是一个独立的函数。
命令行模块使用main.py，其他模块使用src/{模块英文名}.py。
所有的python文件，修改的时候都需要参考对应的模块的设计。
这个项目不使用任何class，所有的功能都是通过函数来实现的。
使用logging模块来记录日志，绝大部分代码（除了main.py）都不应该直接输出到控制台。

## 文档/数据

文档/数据的部分，使用了yaml，尽量使用中文。

## 概念体系

产品管理系统中的所有概念都存储在 `概念/` 目录下，按类别组织成子目录结构。

### 概念分类

| 类别 | 目录 | 说明 |
|------|------|------|
| 元概念 | 概念/元概念/ | 描述概念本身的概念 |
| 产品侧 | 概念/产品侧/ | 产品管理相关的概念 |
| 流程侧 | 概念/流程侧/ | 流程相关的概念 |
| 开发侧 | 概念/开发侧/ | 开发交付相关的概念 |
| 状态 | 概念/状态/ | 描述概念状态的概愁 |
| 基础类型 | 概念/基础类型/ | 基础数据类型定义 |

### 概念列表

#### 元概念
- **概念**：用于描述产品中所有相关概念的元概念

#### 产品侧
- **产品**：代表一个完整的软件产品或项目
- **需求**：定义产品的、模糊的、描述性的文档
- **用户故事**：经过产品经理分析的，确定性比较强的需求描述
- **背景**：定义了一个流程中，不变的因素
- **业务概念**：产品中实际业务相关的专有词汇和术语

#### 流程侧
- **流程**：定义了一个用户故事的具体行为，使用DSL表达
- **步骤**：流程中的基本执行单元
- **步骤类型**：定义了步骤的分类和基本类型
- **条件**：用于在流程中表示条件判断的结构
- **循环**：用于在流程中表示循环执行的结构
- **并行**：用于在流程中表示并行执行的结构
- **结果类型**：流程执行后可能产生的结果类型分类
- **结果**：流程执行后产生的具体结果

#### 开发侧
- **方法**：开发侧所交付的主要概念，用来描述一个具体的功能
- **模块**：开发侧用来管理方法而引入的概念，用于分而治之
- **外部模块**：开发侧所依赖的外部组件，不受本系统直接控制

#### 状态
- **状态**：描述概念所处的当前阶段或条件

#### 基础类型
- **输入**：流程或方法需要接收的数据或信息
- **输出**：流程或方法执行后产生的数据或信息
- **参数**：方法被调用时需要接收的变量
- **返回值**：方法执行完成后返回给调用方的结果

### 概念数据结构规范

每个概念文件必须包含以下字段：

```yaml
名称: xxx
说明: xxx
类别:
  - xxx
关联概念:
  - xxx
详情: |
  xxx
结构:  # 可选，描述概念的数据结构
  - 字段: xxx
    类型: xxx
    必填: true/false
    说明: xxx
```

### 概念之间的关系

概念之间的关系通过 `关联概念` 字段定义，主要包括：

| 关系类型 | 说明 |
|---------|------|
| 组成 | 一个概念由另一个概念组成（如用户故事→流程） |
| 依赖 | 一个概念依赖另一个概念（如流程→背景） |
| 关联 | 两个概念存在某种联系（如需求→用户故事） |

## 项目状态

目前，这个项目处于开发状态。
目标是完全自举，即项目的所有代码和文档都由项目里的产品管理功能完成。

具体定义的流程，可以参考Skill:

- skills/t-pm 用来管理产品管理的相关流程、功能。
