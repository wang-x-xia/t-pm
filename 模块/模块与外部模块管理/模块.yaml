代码: src/module.py
依赖:
- 路径工具
名称: 模块与外部模块管理
方法:
- 从Python文件中提取所有函数名
- 分析模块之间的依赖关系
- 可视化展示模块依赖关系
- 检查所有模块中定义的方法是否都有对应的方法文件
- 检查模块依赖关系的合理性
- 检查模块数据
- 检查模块文件
设计: '模块与外部模块管理模块负责管理系统的模块结构。


  它检查模块数据的完整性、代码文件的存在性、方法文件的映射关系以及模块间的依赖关系。

  是自举检查的核心模块。'
说明: 用来管理所有模块和外部模块的模块
